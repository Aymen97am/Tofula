# Tofula Backend Environment Variables

# Google Generative AI
GOOGLE_API_KEY=your-google-api-key-here

# Database
DATABASE_URL=sqlite:///./tofula.db

# Storage (local, gcs, or firebase)
STORAGE_TYPE=local
LOCAL_STORAGE_PATH=./storage

# Firebase / GCS (if using)
# FIREBASE_PROJECT_ID=your-project-id
# FIREBASE_STORAGE_BUCKET=your-bucket-name
# GCS_BUCKET_NAME=your-gcs-bucket
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account-key.json

# CORS Origins (comma-separated)
# BACKEND_CORS_ORIGINS=http://localhost:3000,http://localhost:3001

# Auth
# JWT_SECRET_KEY=your-secret-key-here
# FIREBASE_WEB_API_KEY=your-firebase-web-api-key

# Model Settings (optional overrides)
# STORY_MODEL=gemini-2.0-flash-exp
# MODERATION_MODEL=gemini-2.0-flash-lite
# POLISH_MODEL=gemini-2.0-flash-exp
# IMAGE_MODEL=gemini-2.5-flash-image
